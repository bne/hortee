[site]
sitename = CHANGEME

[django]
settings = production

[apache-base]
sitename = ${site:sitename}
serveradmin = example@CHANGEME
aliases = /static/:${buildout:cwd}/src/${django:project}/static
	  /media/:${buildout:parts-directory}/django/django/contrib/admin/media/

