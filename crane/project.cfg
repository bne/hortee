[buildout]
eggs = south

[django]
project = testproject
apps = testapp
settings = settings

test = ${django:apps}
projectegg = ${django:project}
eggs = ${buildout:eggs}
external_apps = south
server_email = root@localhost

[versions]
south = 0.7.2
