[buildout]
eggs = south

[django]
project = hortee
apps = main
settings = settings

test = ${django:apps}
projectegg = ${django:project}
eggs = ${buildout:eggs}
external_apps = south
server_email = root@localhost

[versions]
south = 0.7.2

